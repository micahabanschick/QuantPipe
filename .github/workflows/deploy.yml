name: Deploy to Server

on:
  push:
    branches: [laptop-dev, main]

jobs:
  deploy:
    name: Pull & restart Streamlit
    runs-on: self-hosted          # executes on the VPN server, no inbound port needed

    steps:
      - name: Pull latest code and restart dashboard
        run: sudo /opt/quantpipe/deploy/update.sh
