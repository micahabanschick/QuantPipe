"""Research layer — pure analytics modules, no Streamlit or I/O dependencies.

Modules:
  signal_scanner      — factor snapshot, z-scores, universe rankings
  factor_analysis     — time-series stats, distribution, information coefficient
  walk_forward_runner — WFV orchestration wrapper around backtest.walk_forward
"""
